#include<stdio.h>
#include<conio.h>
main(){
	int size,i,temp;
	int a[size];
	printf("\n Enter size :");
	scanf("%d",&size);
	for(i=0;i<size;i++){
		printf("Enter no. :");
		scanf("%d",&a[i]);
		printf("no.s %d :",a[i]);
		temp=a[i];
		a[i]
	}
	getch();
}
